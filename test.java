import java.util.*;

public class test {
    public static void main(String[] args) {
        // int[] arr = {1024,1024,1024,1024,1024,1024,1024,1024,1024,1024};
        // int m = 10;
        // PriorityQueue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder());
        // int totalSum = 0;
        // for (int val : arr) {
        //     totalSum += val;
        //     pq.offer(val);
        // }

        // while (m!=0) {
        //     int maxItem = pq.poll();
        //     int save = maxItem/2;
        //     totalSum -= save;
        //     pq.offer(save);
        //     m -= 1;
        // }
        // System.out.println(totalSum);
        

    }
}